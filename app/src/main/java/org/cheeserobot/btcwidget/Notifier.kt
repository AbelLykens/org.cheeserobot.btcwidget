package org.cheeserobot.btcwidget

// Notifier was removed in v1.3 — fetch errors are now surfaced inside
// the launcher activity (LauncherActivity) instead of via system
// notifications, and the POST_NOTIFICATIONS permission is no longer
// required. This file is intentionally empty; once the build system
// allows source deletion it can go away entirely.
